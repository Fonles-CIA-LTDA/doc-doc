'use strict';

/**
 * especialidades-flash-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::especialidades-flash-card.especialidades-flash-card');
