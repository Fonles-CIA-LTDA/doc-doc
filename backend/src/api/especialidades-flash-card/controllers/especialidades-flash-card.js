'use strict';

/**
 * especialidades-flash-card controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::especialidades-flash-card.especialidades-flash-card');
