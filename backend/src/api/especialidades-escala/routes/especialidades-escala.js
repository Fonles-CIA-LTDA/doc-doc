'use strict';

/**
 * especialidades-escala router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::especialidades-escala.especialidades-escala');
