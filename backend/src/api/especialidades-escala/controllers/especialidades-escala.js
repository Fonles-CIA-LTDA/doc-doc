'use strict';

/**
 * especialidades-escala controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::especialidades-escala.especialidades-escala');
