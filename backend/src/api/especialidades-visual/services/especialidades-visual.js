'use strict';

/**
 * especialidades-visual service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::especialidades-visual.especialidades-visual');
